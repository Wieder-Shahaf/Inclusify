---
phase: 05.4.1
plan: 01
type: execute
wave: 1
depends_on: [05.4.1-00]
files_modified:
  - ml/data_synthesis/__init__.py
  - ml/data_synthesis/config.py
  - ml/data_synthesis/synthesize_english.py
  - ml/data_synthesis/utils/__init__.py
  - ml/data_synthesis/utils/batch_processor.py
  - ml/data_synthesis/prompts/english_variations.txt
  - ml/data_synthesis/tests/test_synthesize_english.py
  - ml/data_synthesis/tests/test_batch_api.py
  - data/intermediate/english_raw_11k.jsonl
  - data/english_10k.csv
autonomous: false
requirements: [LLM-02, LANG-01]

must_haves:
  truths:
    - "English dataset contains 10,000+ unique samples before deduplication"
    - "Generated samples maintain academic tone and LGBTQ+ domain focus"
    - "Class distribution preserved (~20% appropriate, ~80% problematic)"
    - "Batch API processing completes without manual intervention"
  artifacts:
    - path: "ml/data_synthesis/synthesize_english.py"
      provides: "Main orchestration script for English dataset expansion"
      min_lines: 150
    - path: "ml/data_synthesis/utils/batch_processor.py"
      provides: "OpenAI/Claude Batch API wrapper"
      exports: ["BatchProcessor", "submit_batch", "poll_results"]
    - path: "data/english_10k.csv"
      provides: "Final English dataset (before quality validation)"
      min_lines: 10000
    - path: "ml/data_synthesis/prompts/english_variations.txt"
      provides: "System prompts for LLM generation"
  key_links:
    - from: "ml/data_synthesis/synthesize_english.py"
      to: "ml/data_synthesis/utils/batch_processor.py"
      via: "import and call submit_batch()"
      pattern: "from.*batch_processor import.*submit_batch"
    - from: "ml/data_synthesis/utils/batch_processor.py"
      to: "OpenAI Batch API"
      via: "httpx or openai library"
      pattern: "openai\\.Client|client\\.batches\\.create"
---

<objective>
Expand the existing 1,001-sample English dataset to 10,000+ high-quality samples using LLM-based synthesis via OpenAI or Claude Batch APIs.

Purpose: Increase dataset diversity and size for better model generalization without manual data collection. Current 988 unique samples (after deduplication) are insufficient for robust LGBTQ+ inclusive language detection across diverse academic contexts.

Output:
- English synthesis pipeline using Batch APIs (50% cost savings vs synchronous)
- 10,000+ generated samples in data/english_10k.csv
- Intermediate JSONL results for debugging
</objective>

<execution_context>
@/Users/shahafwieder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shahafwieder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4.1-dataset-synthesis-scale-to-10k-samples-hebrew-equivalent/05.4.1-CONTEXT.md
@.planning/phases/05.4.1-dataset-synthesis-scale-to-10k-samples-hebrew-equivalent/05.4.1-RESEARCH.md
@data/augmented_dataset.csv
@ml/training/prepare_data.py
</context>

<interfaces>
<!-- Key types from existing codebase -->

From data/augmented_dataset.csv:
```csv
Sentence,Severity Label,Explanation
"Gender identity and sexual orientation are distinct concepts.",Correct,"Accurate distinction..."
"Homosexual lifestyle is a choice.",Factually Incorrect,"Sexual orientation is not a choice..."
```

From ml/training/prepare_data.py:
```python
def load_dataset(path: str) -> pd.DataFrame:
    """Loads CSV, removes duplicates, returns DataFrame with columns: Sentence, Severity Label, Explanation"""
    pass

def stratified_split(df: pd.DataFrame, test_size: float = 0.2, random_state: int = 42):
    """Returns train_df, val_df preserving class proportions"""
    pass
```

Expected output format (same schema as augmented_dataset.csv):
- Sentence (str): Academic text sample
- Severity Label (str): One of [Correct, Outdated, Biased, Potentially Offensive, Factually Incorrect]
- Explanation (str): Reasoning for classification
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create data synthesis package structure and configuration</name>
  <files>
    ml/data_synthesis/__init__.py
    ml/data_synthesis/config.py
    ml/data_synthesis/utils/__init__.py
    ml/data_synthesis/prompts/english_variations.txt
  </files>
  <action>
    Create ml/data_synthesis/ package structure:

    1. Create __init__.py files for Python package structure

    2. Create config.py with:
       - API configuration (OpenAI or Claude - use OpenAI by default per research)
       - Environment variables: OPENAI_API_KEY, BATCH_SIZE (default 2000 per chunk)
       - Generation parameters: MODEL="gpt-4o", MAX_TOKENS=500, TEMPERATURE=0.9 (higher temp for diversity)
       - Target counts: TOTAL_TARGET=11000 (10% buffer for deduplication), per-class targets from stratified sampling
       - Paths: INPUT_CSV="data/augmented_dataset.csv", OUTPUT_CSV="data/english_10k.csv", INTERMEDIATE_DIR="data/intermediate/"

    3. Create prompts/english_variations.txt with system prompt:
       - Role: Expert academic editor for Inclusify project
       - Task: Generate diverse variations of LGBTQ+ inclusive language samples
       - Constraints: Maintain academic tone, preserve severity classification, vary contexts (psychology, sociology, medicine, education, law)
       - Output format: JSON with {sentence, severity_label, explanation}
       - Diversity guidance: Change framing, terminology, academic domain, sentence structure (not just synonym replacement)

    Use research patterns: stratified generation (calculate per-class targets), diverse prompt variations to prevent repetitive output.
  </action>
  <verify>
    <automated>python -c "from ml.data_synthesis.config import TOTAL_TARGET, MODEL; assert TOTAL_TARGET == 11000; assert MODEL == 'gpt-4o'"</automated>
  </verify>
  <done>Config module imports successfully, prompts/english_variations.txt contains system prompt with diversity guidance, package structure ready for synthesis scripts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Batch API processor with retry logic</name>
  <files>
    ml/data_synthesis/utils/batch_processor.py
    ml/data_synthesis/tests/test_batch_api.py
  </files>
  <behavior>
    - Test 1: submit_batch() creates valid JSONL and returns batch_id
    - Test 2: poll_results() waits for completion and returns parsed results
    - Test 3: Retry logic handles transient API failures (max 3 retries)
    - Test 4: Malformed JSONL raises clear validation error
    - Test 5: Batch job failure returns error details in results
  </behavior>
  <action>
    After tests pass, implement batch_processor.py:

    1. BatchProcessor class:
       - __init__(api_key: str, model: str): Initialize OpenAI client
       - submit_batch(requests: List[dict], custom_id_prefix: str) -> str:
         * Validate JSONL format (no trailing commas, proper escaping)
         * Write to temp file, upload via client.files.create()
         * Create batch job via client.batches.create(completion_window="24h")
         * Return batch.id
       - poll_results(batch_id: str, poll_interval: int = 60) -> List[dict]:
         * Poll batch status every 60 seconds
         * Handle status: completed (parse results), failed (return errors), cancelled (raise exception)
         * Download results file, parse JSONL
         * Return list of {custom_id, result, error} dicts

    2. Helper functions:
       - validate_jsonl(requests: List[dict]) -> None: Check format before upload
       - parse_batch_results(file_content: str) -> List[dict]: Parse response JSONL

    Use research patterns: OpenAI Batch API format (custom_id, method, url, body), 24h completion window, polling with timeout.
    Error handling: Wrap API calls in try-except, log errors, continue processing.
  </action>
  <verify>
    <automated>pytest ml/data_synthesis/tests/test_batch_api.py -v -x</automated>
  </verify>
  <done>All batch processing tests pass, BatchProcessor can submit batches and poll for results, error handling validated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create English synthesis orchestration script</name>
  <files>
    ml/data_synthesis/synthesize_english.py
    ml/data_synthesis/tests/test_synthesize_english.py
  </files>
  <behavior>
    - Test 1: load_existing_dataset() returns 988 unique samples
    - Test 2: calculate_generation_targets() produces stratified per-class targets totaling ~11K
    - Test 3: generate_batch_requests() creates valid JSONL with proper schema
    - Test 4: parse_and_save_results() validates output schema and filters invalid samples
  </behavior>
  <action>
    After tests pass, implement main orchestration script:

    1. load_existing_dataset():
       - Use ml.training.prepare_data.load_dataset() to load augmented_dataset.csv
       - Returns DataFrame with 988 unique samples after deduplication

    2. calculate_generation_targets():
       - Calculate per-class targets to reach 11,000 total samples (10% buffer)
       - Use stratified sampling: target_per_class = (current_class_count / 988) * 11000
       - Return dict: {severity_label: samples_to_generate}

    3. generate_batch_requests():
       - For each severity class, sample existing sentences as seed
       - Create batch request per seed: {"custom_id": f"{class}_{idx}", "method": "POST", "url": "/v1/chat/completions", "body": {...}}
       - Body includes system prompt from english_variations.txt + user prompt with seed sentence
       - Randomize prompt variations: "change domain to sociology", "shift framing to activist", "use clinical terminology"
       - Return list of requests (split into chunks of 2000 for multiple batches)

    4. submit_and_poll_batches():
       - Split requests into chunks (BATCH_SIZE=2000 from config)
       - For each chunk: submit_batch(), wait for completion via poll_results()
       - Save intermediate results to data/intermediate/english_raw_11k.jsonl
       - Aggregate all results into single list

    5. parse_and_save_results():
       - Extract generated samples from batch results
       - Parse JSON responses: {sentence, severity_label, explanation}
       - Validate: sentence non-empty, severity_label in allowed set, explanation present
       - Filter invalid samples (log count)
       - Save to data/english_10k.csv (before deduplication - Plan 03 handles that)

    6. main():
       - Orchestrate all steps: load → calculate → generate → submit → parse → save
       - Print progress: "Loaded 988 samples", "Generating 10,012 new samples", "Batch 1/5 submitted", "Batch 1/5 completed"
       - Log statistics: total generated, per-class counts, invalid samples filtered

    Use research patterns: Stratified generation, batch chunking for <24h completion, intermediate saves, robust error handling.
  </action>
  <verify>
    <automated>pytest ml/data_synthesis/tests/test_synthesize_english.py -v -x</automated>
  </verify>
  <done>Synthesis script tests pass, main() function orchestrates full pipeline, intermediate results saving implemented, ready for execution</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Execute English synthesis and verify output</name>
  <what-built>
    Claude will execute synthesize_english.py to generate 11K raw English samples via OpenAI Batch API:
    - Load 988 existing samples from augmented_dataset.csv
    - Calculate stratified generation targets (10,012 new samples preserving class proportions)
    - Submit batch requests to OpenAI API (chunked into batches of 2000)
    - Poll for completion (may take several hours for Batch API processing)
    - Parse results and save to data/english_10k.csv
    - Save intermediate JSONL to data/intermediate/english_raw_11k.jsonl
  </what-built>
  <how-to-verify>
    1. Check synthesize_english.py execution output:
       - Confirms "Loaded 988 samples"
       - Shows batch submission progress (e.g., "Batch 1/5 submitted")
       - Reports completion for each batch
       - Shows final count: "Generated 10,144 raw samples" (or similar)

    2. Verify data/english_10k.csv exists:
       ```bash
       wc -l data/english_10k.csv  # Should show ~10,000-11,000 lines
       head -20 data/english_10k.csv  # Verify CSV structure and content
       ```

    3. Check intermediate results:
       ```bash
       ls -lh data/intermediate/english_raw_11k.jsonl  # Should exist with non-zero size
       ```

    4. Spot-check quality:
       - Open data/english_10k.csv in spreadsheet or text editor
       - Randomly sample 10-20 rows
       - Verify: Sentences are academic, severity labels are appropriate, explanations make sense
       - Check for variety (not repetitive phrasing)

    Expected outcomes:
    - data/english_10k.csv exists with 10,000+ rows
    - All rows have valid schema: Sentence, Severity Label, Explanation
    - Samples show diversity in phrasing and academic domains
    - Class proportions roughly match original dataset (~20% appropriate, ~80% problematic)
  </how-to-verify>
  <resume-signal>
    Type "approved" if data/english_10k.csv exists with valid samples, OR describe issues if generation failed or quality is poor.
  </resume-signal>
</task>

</tasks>

<verification>
Package structure complete:
- ml/data_synthesis/ exists with __init__.py
- config.py has TOTAL_TARGET, MODEL, API configuration
- batch_processor.py exports BatchProcessor, submit_batch, poll_results
- synthesize_english.py has main() function ready to execute

Tests pass:
- pytest ml/data_synthesis/tests/test_batch_api.py passes all tests
- pytest ml/data_synthesis/tests/test_synthesize_english.py passes all tests

Execution:
- synthesize_english.py executed via checkpoint task
- data/english_10k.csv created with 10,000+ samples
- Intermediate JSONL saved for debugging

Human verification:
- User confirms dataset quality and completeness
- Checkpoint gate allows continuation to Plan 02
</verification>

<success_criteria>
- ml/data_synthesis/ package structure created with config, utils, prompts
- BatchProcessor implemented with submit_batch() and poll_results()
- synthesize_english.py orchestrates: load → calculate → generate → submit → parse → save
- All tests pass (batch_processing and synthesis tests)
- synthesize_english.py executed successfully
- data/english_10k.csv exists with 10,000+ samples
- Intermediate results directory structure created (data/intermediate/)
- Human verification confirms dataset quality
</success_criteria>

<output>
After completion, create `.planning/phases/05.4.1-dataset-synthesis-scale-to-10k-samples-hebrew-equivalent/05.4.1-01-SUMMARY.md`

Include:
- Package structure created
- Batch API integration patterns used
- Stratified generation strategy
- Files created and their purposes
- Test results
- Synthesis execution results (samples generated, time taken, any issues)
- Dataset quality spot-check summary
- Ready for Plan 02 (Hebrew translation)
</output>
