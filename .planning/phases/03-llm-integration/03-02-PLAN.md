---
phase: 03-llm-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/modules/analysis/llm_client.py
  - backend/app/modules/analysis/circuit_breaker.py
  - backend/app/modules/analysis/sentence_splitter.py
  - backend/app/core/config.py
  - backend/requirements.txt
  - backend/tests/test_llm_client.py
autonomous: true
requirements: [LLM-02]

must_haves:
  truths:
    - "vLLM client can call /v1/chat/completions endpoint"
    - "Circuit breaker opens after 3 consecutive failures"
    - "Circuit breaker auto-recovers after 60 seconds"
    - "Sentences are split with accurate character offsets"
    - "Hebrew sentences split correctly (not mid-word)"
  artifacts:
    - path: "backend/app/modules/analysis/llm_client.py"
      provides: "Async HTTP client for vLLM with timeout"
      exports: ["VLLMClient", "analyze_sentence"]
      min_lines: 50
    - path: "backend/app/modules/analysis/circuit_breaker.py"
      provides: "pybreaker-based circuit breaker"
      contains: "CircuitBreaker"
    - path: "backend/app/modules/analysis/sentence_splitter.py"
      provides: "pysbd-based sentence splitting with offsets"
      contains: "split_with_offsets"
    - path: "backend/tests/test_llm_client.py"
      provides: "Unit tests for LLM client, circuit breaker, sentence splitter"
      min_lines: 80
  key_links:
    - from: "backend/app/modules/analysis/llm_client.py"
      to: "circuit_breaker.py"
      via: "decorator"
      pattern: "@.*breaker"
    - from: "backend/app/modules/analysis/llm_client.py"
      to: "config.py"
      via: "settings import"
      pattern: "from app.core.config import"
---

<objective>
Create vLLM HTTP client with circuit breaker protection and sentence splitting utility.

Purpose: Provide robust infrastructure for LLM inference calls that fails gracefully under pressure.
Output: Tested modules for vLLM communication, circuit breaker, and sentence boundary detection.
</objective>

<execution_context>
@/Users/shahafwieder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shahafwieder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-llm-integration/03-CONTEXT.md
@.planning/phases/03-llm-integration/03-RESEARCH.md

# Existing code
@backend/app/core/config.py
@backend/app/modules/analysis/router.py
@ml/inference_demo.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/core/config.py (to extend):
```python
class Settings(BaseSettings):
    # Existing fields...
    JWT_SECRET: str
    REDIS_URL: str
    DATABASE_URL: Optional[str]
```

From ml/inference_demo.py (system prompt to reuse):
```python
SYSTEM_PROMPT = """You are an expert academic editor for the Inclusify project. Analyze sentences for LGBTQ+ inclusive language compliance.
OUTPUT REQUIREMENTS:
You MUST respond with ONLY a valid JSON object. No other text, no markdown, no explanation outside the JSON.
STRICT JSON SCHEMA:
{
  "category": "<rule category>",
  "severity": "<EXACTLY one of: 'Correct', 'Outdated', 'Biased', 'Potentially Offensive', 'Factually Incorrect'>",
  "explanation": "<detailed reasoning>"
}
"""
```

From backend/app/modules/analysis/router.py (Issue model):
```python
class Issue(BaseModel):
    span: str
    severity: Literal['outdated', 'biased', 'offensive', 'incorrect']
    type: str
    description: str
    suggestion: Optional[str] = None
    start: int
    end: int
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add vLLM settings and dependencies</name>
  <files>backend/app/core/config.py, backend/requirements.txt</files>
  <behavior>
    - Settings.VLLM_URL defaults to "http://localhost:8001"
    - Settings.VLLM_TIMEOUT defaults to 30.0
    - Settings.VLLM_CIRCUIT_FAIL_MAX defaults to 3
    - Settings.VLLM_CIRCUIT_RESET_TIMEOUT defaults to 60
    - pybreaker and pysbd added to requirements.txt
  </behavior>
  <action>
1. Update backend/app/core/config.py to add vLLM settings:
   - VLLM_URL: str = "http://localhost:8001"
   - VLLM_TIMEOUT: float = 30.0
   - VLLM_CIRCUIT_FAIL_MAX: int = 3
   - VLLM_CIRCUIT_RESET_TIMEOUT: int = 60

2. Update backend/requirements.txt to add:
   - pybreaker>=1.2.0
   - pysbd>=0.3.4
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/inclusify/backend && python -c "from app.core.config import settings; print(settings.VLLM_URL)"</automated>
  </verify>
  <done>Settings include VLLM_URL, VLLM_TIMEOUT, circuit breaker params. Dependencies added to requirements.txt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create circuit breaker module</name>
  <files>backend/app/modules/analysis/circuit_breaker.py, backend/tests/test_llm_client.py</files>
  <behavior>
    - Circuit breaker opens after fail_max (3) consecutive failures
    - Circuit breaker stays open for reset_timeout (60s)
    - Circuit breaker auto-closes (half-open) after timeout
    - Successful call in half-open state closes circuit
    - CircuitBreakerError raised when circuit is open
  </behavior>
  <action>
1. Create backend/app/modules/analysis/circuit_breaker.py:
   - Import pybreaker
   - Import settings from app.core.config
   - Create vllm_breaker as CircuitBreaker instance with:
     - fail_max=settings.VLLM_CIRCUIT_FAIL_MAX
     - reset_timeout=settings.VLLM_CIRCUIT_RESET_TIMEOUT
     - state_storage=CircuitMemoryStorage()
   - Export vllm_breaker and CircuitBreakerError for use in client

2. Create initial test file backend/tests/test_llm_client.py with circuit breaker tests:
   - test_circuit_breaker_opens_after_failures: Call failing function 3 times, verify circuit opens
   - test_circuit_breaker_auto_recovers: Mock time passage, verify half-open state
   - Use pytest fixtures to create fresh breaker per test (avoid cross-test state)
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/inclusify/backend && python -m pytest tests/test_llm_client.py::test_circuit_breaker_opens_after_failures -x -v 2>/dev/null || python -c "from app.modules.analysis.circuit_breaker import vllm_breaker; print('Module imports OK')"</automated>
  </verify>
  <done>circuit_breaker.py exports vllm_breaker with correct config. Tests verify open/recover behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create sentence splitter module</name>
  <files>backend/app/modules/analysis/sentence_splitter.py, backend/tests/test_llm_client.py</files>
  <behavior>
    - split_with_offsets("Hello. World.", "en") returns [("Hello.", 0, 6), ("World.", 7, 13)]
    - Hebrew text splits correctly without breaking mid-word
    - Empty string returns empty list
    - Single sentence returns list with one tuple
    - Preserves original text (no cleaning)
  </behavior>
  <action>
1. Create backend/app/modules/analysis/sentence_splitter.py:
   - Import pysbd
   - Create function split_with_offsets(text: str, language: str = 'en') -> list[tuple[str, int, int]]
   - Use pysbd.Segmenter(language=language, clean=False) per RESEARCH.md
   - Track offsets by finding each sentence in original text
   - Handle edge cases: empty text, single sentence

2. Add tests to backend/tests/test_llm_client.py:
   - test_sentence_split_english: Basic English splitting with offsets
   - test_sentence_split_hebrew: Hebrew text with proper boundaries
   - test_sentence_split_empty: Empty string handling
   - test_sentence_split_single: Single sentence returns one tuple
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/inclusify/backend && python -m pytest tests/test_llm_client.py -k "sentence" -x -v 2>/dev/null || python -c "from app.modules.analysis.sentence_splitter import split_with_offsets; print(split_with_offsets('Hello. World.', 'en'))"</automated>
  </verify>
  <done>sentence_splitter.py correctly splits English and Hebrew text with accurate character offsets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create vLLM client module</name>
  <files>backend/app/modules/analysis/llm_client.py, backend/tests/test_llm_client.py</files>
  <behavior>
    - VLLMClient.analyze_sentence returns parsed JSON or None on failure
    - Returns None (not raises) on timeout or HTTP error
    - Circuit breaker decorator protects the call
    - Parses LLM JSON output using regex pattern from inference_demo.py
    - Maps severity: "Outdated"->outdated, "Biased"->biased, "Potentially Offensive"->offensive, "Factually Incorrect"->incorrect
  </behavior>
  <action>
1. Create backend/app/modules/analysis/llm_client.py:
   - Import httpx, json, re
   - Import vllm_breaker from circuit_breaker
   - Import settings from app.core.config

   - SYSTEM_PROMPT constant (copy from ml/inference_demo.py)

   - Function parse_llm_output(raw: str) -> dict | None:
     - Extract JSON from markdown code blocks if present
     - Find JSON boundaries { ... }
     - Parse and return dict, or None on failure
     - Pattern from inference_demo.py

   - Function map_severity(llm_severity: str) -> str:
     - "Outdated" -> "outdated"
     - "Biased" -> "biased"
     - "Potentially Offensive" -> "offensive"
     - "Factually Incorrect" -> "incorrect"
     - "Correct" -> skip (return None to indicate skip)

   - Class VLLMClient:
     - __init__(self, base_url: str = None, timeout: float = None)
     - Use settings defaults if not provided
     - Create httpx.AsyncClient with timeout
     - Method analyze_sentence(self, sentence: str) -> dict | None:
       - Decorated with @vllm_breaker
       - POST to {base_url}/v1/chat/completions
       - Request body: model, messages (system + user), max_tokens=256, temperature=0.1
       - Parse response with parse_llm_output
       - Return parsed dict with mapped severity, or None on any error
       - Catch httpx.TimeoutException, httpx.HTTPStatusError, pybreaker.CircuitBreakerError

2. Add tests to backend/tests/test_llm_client.py:
   - test_parse_llm_output_valid: Valid JSON parsing
   - test_parse_llm_output_markdown: JSON in markdown code block
   - test_parse_llm_output_invalid: Returns None for invalid JSON
   - test_map_severity: All severity mappings
   - test_vllm_client_success: Mock httpx, verify request format
   - test_vllm_client_timeout: Mock timeout, verify returns None
   - test_vllm_client_circuit_open: Verify returns None when circuit open
  </action>
  <verify>
    <automated>cd /Users/shahafwieder/inclusify/backend && python -m pytest tests/test_llm_client.py -x -v 2>/dev/null || python -c "from app.modules.analysis.llm_client import VLLMClient, parse_llm_output, map_severity; print('LLM client imports OK')"</automated>
  </verify>
  <done>VLLMClient calls vLLM with correct format, handles errors gracefully, circuit breaker protects calls.</done>
</task>

</tasks>

<verification>
- All tests in test_llm_client.py pass
- Circuit breaker opens after 3 failures
- Sentence splitter handles Hebrew correctly
- vLLM client parses JSON responses
- Timeouts return None (no exceptions bubble up)
</verification>

<success_criteria>
- VLLMClient can call analyze_sentence with proper request format
- Circuit breaker protects against cascade failures
- Sentence splitter provides accurate character offsets
- All edge cases handled (timeouts, parse errors, empty input)
- Test coverage for all new modules
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm-integration/03-02-SUMMARY.md`
</output>
