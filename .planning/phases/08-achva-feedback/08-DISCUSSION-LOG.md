# Phase 8: Achva Feedback - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-18
**Phase:** 08-achva-feedback
**Areas discussed:** Profile popup, LLM-down fallback, PDF watermark, Contact us mechanics, Admin frequency trends

---

## Profile popup (D-01)

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — show every session until complete | sessionStorage resets per tab/session; popup re-appears until profile complete | ✓ |
| One-time dismiss — never show again | localStorage; dismissed once means never again | |
| Always show until complete, no dismiss | No X button; must fill in to close | |

**User's choice:** Show every session until complete (sessionStorage)

| Option | Description | Selected |
|--------|-------------|----------|
| full_name only | Current logic | |
| full_name + institution | Both required | |
| All three: full_name, institution, profession | Strictest | ✓ |

**User's choice:** All three fields required (full_name, institution, profession)

---

## LLM-down fallback (D-02)

| Option | Description | Selected |
|--------|-------------|----------|
| Create a new /glossary page | Build a new route with the term dictionary | |
| Existing /[locale]/glossary page | Link to the already-built glossary route | ✓ |
| External URL | Point outside the app | |

**User's choice:** Existing /[locale]/glossary page (confirmed it exists)

| Option | Description | Selected |
|--------|-------------|----------|
| "Analysis service is temporarily unavailable. Browse the glossary for guidance." | Recommended | ✓ |
| "Our AI analysis is offline. You can still review our inclusive language glossary." | More conversational | |
| Custom | User-specified text | |

**User's choice:** "Analysis service is temporarily unavailable. Browse the glossary for guidance."

---

## PDF watermark (D-03)

| Option | Description | Selected |
|--------|-------------|----------|
| "INCLUSIFY" | Brand name only | |
| "Generated by Inclusify" | Slightly descriptive | |
| "Achva LGBTQ+ Studential Organization" | Credits organization | ✓ |

**User's choice:** "Achva LGBTQ+ Studential Organization" (EN) / "ארגון אחווה להט״ב הסטודנטיאלי" (HE) — locale-driven

| Option | Description | Selected |
|--------|-------------|----------|
| Diagonal across center, low opacity | Classic watermark | |
| Bottom footer on each page | Small text at bottom of every page | ✓ |
| Header area only on first page | First page top only | |

**User's choice:** Bottom footer on each page

---

## Contact us mechanics (D-04)

| Option | Description | Selected |
|--------|-------------|----------|
| Results page only, near Export button | Contextual placement | |
| Navigation bar (always visible) | Global access | ✓ |
| Both results page and /contact page | Most discoverable | |

**User's choice:** Navigation bar (always visible)

| Option | Description | Selected |
|--------|-------------|----------|
| Backend API POST /api/v1/contact | smtplib, Gmail App Password | ✓ |
| mailto: link | Opens user email client | |
| Slack/Teams webhook | Posts to admin channel | |

**User's choice:** Backend API — smtplib (stdlib), Gmail + App Password, query admin emails from DB, optional PDF attachment

**User's notes:** "Backend: Add SMTP config (Gmail + App Password), new contact router that queries admin emails from DB, sends via smtplib with optional PDF attachment. Frontend: Extend exportReport.ts to return base64 instead of downloading, add API client, update handleContactUs to call backend (with PDF if analysis done, predefined template if not). No new dependencies — smtplib is stdlib."

| Option | Description | Selected |
|--------|-------------|----------|
| Modal with subject + message, auto-prefilled | Recommended | ✓ |
| One-click send, no form | Immediate POST | |
| Dedicated /contact page | New route | |

**User's choice:** Modal with subject + message, auto-prefilled with name, email, institution from AuthContext

**Notes:** PDF attachment = exportReport.ts in base64-return mode when analysis is complete. Coordinate with Adan's branch (PDF export in progress on separate branch).

---

## Admin frequency trends (D-05)

| Option | Description | Selected |
|--------|-------------|----------|
| Bar chart: top-5 flagged terms | Horizontal bars, hit counts | |
| Line chart: term frequency over time | Trend direction | |
| Both: bar + line | Most complete | |

**User's choice:** Bar chart per label (category) showing frequency + dropdown per label with top-5 phrases

**User's notes:** "a bar chart for each label show its frequency and also a drop-down list that show the top 5 phrases in each label. (we don't have phrase level labeling so we will need to get from the llm in analysis time an index or some mapping to the phrase that is problematic in the labeled sentence and save it in db and then show it in the admin dashboard via sql)"

**Note from Claude:** `findings.excerpt_redacted` already stores the flagged phrase text — no new schema changes needed. Top-5 query can use `GROUP BY category, excerpt_redacted`.

| Option | Description | Selected |
|--------|-------------|----------|
| Poll every 30 seconds | Simple setInterval | |
| Refresh on page load / manual button | No auto-refresh | |
| WebSocket push from backend | Real-time | ✓ |

**User's choice:** WebSocket push from backend on new analysis completion

---

## Claude's Discretion

- Hebrew translations for new i18n strings
- WebSocket library choice (FastAPI native)
- Contact modal component structure
- Retry/error handling in contact endpoint

## Deferred Ideas

None noted during discussion.
