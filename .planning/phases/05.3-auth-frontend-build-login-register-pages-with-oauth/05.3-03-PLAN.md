---
phase: 05.3-auth-frontend
plan: 03
type: execute
wave: 2
depends_on:
  - 05.3-01
files_modified:
  - frontend/components/auth/UserAvatar.tsx
  - frontend/components/auth/UserDropdown.tsx
  - frontend/components/ui/dropdown-menu.tsx
  - frontend/components/Navbar.tsx
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "Logged out users see 'Login' text button in navbar"
    - "Logged in users see avatar with dropdown menu"
    - "Avatar shows user initials on colored background"
    - "Dropdown contains Profile, My Analyses, Logout options"
    - "Admin link is hidden for non-admin users"
  artifacts:
    - path: "frontend/components/auth/UserAvatar.tsx"
      provides: "Avatar component with initials fallback"
      exports: ["UserAvatar"]
    - path: "frontend/components/auth/UserDropdown.tsx"
      provides: "Dropdown menu with auth actions"
      exports: ["UserDropdown"]
    - path: "frontend/components/ui/dropdown-menu.tsx"
      provides: "Radix dropdown menu wrapper"
      exports: ["DropdownMenu", "DropdownMenuTrigger", "DropdownMenuContent", "DropdownMenuItem"]
  key_links:
    - from: "frontend/components/Navbar.tsx"
      to: "frontend/components/auth/UserDropdown.tsx"
      via: "conditional rendering based on user"
      pattern: "user.*UserDropdown"
    - from: "frontend/components/auth/UserDropdown.tsx"
      to: "frontend/contexts/AuthContext.tsx"
      via: "useAuth hook"
      pattern: "useAuth\\(\\)"
---

<objective>
Integrate auth state into the navbar: show Login button when logged out, avatar dropdown when logged in. Hide admin link for non-admins.

Purpose: Users can see their auth state and access account actions from any page.
Output: Navbar displays appropriate auth UI based on login state.
</objective>

<execution_context>
@/Users/shahafwieder/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shahafwieder/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.3-auth-frontend-build-login-register-pages-with-oauth/05.3-CONTEXT.md
@.planning/phases/05.3-auth-frontend-build-login-register-pages-with-oauth/05.3-RESEARCH.md
@.planning/phases/05.3-auth-frontend-build-login-register-pages-with-oauth/05.3-01-SUMMARY.md

<interfaces>
<!-- From 05.3-01 AuthContext -->
import { useAuth } from '@/contexts/AuthContext';

interface User {
  id: string;
  email: string;
  role: string;
}

const { user, isLoading, logout } = useAuth();

<!-- Existing Navbar structure (from frontend/components/Navbar.tsx) -->
- navLinks array with href and key
- Uses useTranslations('app')
- Uses useLocale() and usePathname()
- Has btn-primary class for CTA button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Radix dropdown menu wrapper</name>
  <files>frontend/components/ui/dropdown-menu.tsx</files>
  <action>
Create shadcn-style wrapper for @radix-ui/react-dropdown-menu.

**Create `frontend/components/ui/dropdown-menu.tsx`:**

```typescript
'use client';

import * as React from 'react';
import * as DropdownMenuPrimitive from '@radix-ui/react-dropdown-menu';
import { cn } from '@/lib/utils';

const DropdownMenu = DropdownMenuPrimitive.Root;
const DropdownMenuTrigger = DropdownMenuPrimitive.Trigger;
const DropdownMenuGroup = DropdownMenuPrimitive.Group;

const DropdownMenuContent = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Content>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Content>
>(({ className, sideOffset = 4, ...props }, ref) => (
  <DropdownMenuPrimitive.Portal>
    <DropdownMenuPrimitive.Content
      ref={ref}
      sideOffset={sideOffset}
      className={cn(
        'z-50 min-w-[180px] overflow-hidden rounded-lg border border-slate-200 bg-white p-1 shadow-lg',
        'dark:border-slate-700 dark:bg-slate-900',
        'data-[state=open]:animate-in data-[state=closed]:animate-out',
        'data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0',
        'data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95',
        'data-[side=bottom]:slide-in-from-top-2 data-[side=top]:slide-in-from-bottom-2',
        className
      )}
      {...props}
    />
  </DropdownMenuPrimitive.Portal>
));
DropdownMenuContent.displayName = DropdownMenuPrimitive.Content.displayName;

const DropdownMenuItem = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Item>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Item> & {
    inset?: boolean;
  }
>(({ className, inset, ...props }, ref) => (
  <DropdownMenuPrimitive.Item
    ref={ref}
    className={cn(
      'relative flex cursor-pointer select-none items-center gap-2 rounded-md px-3 py-2 text-sm outline-none transition-colors',
      'text-slate-700 dark:text-slate-200',
      'focus:bg-slate-100 focus:text-slate-900 dark:focus:bg-slate-800 dark:focus:text-slate-100',
      'data-[disabled]:pointer-events-none data-[disabled]:opacity-50',
      inset && 'ps-8',
      className
    )}
    {...props}
  />
));
DropdownMenuItem.displayName = DropdownMenuPrimitive.Item.displayName;

const DropdownMenuSeparator = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Separator>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Separator>
>(({ className, ...props }, ref) => (
  <DropdownMenuPrimitive.Separator
    ref={ref}
    className={cn('-mx-1 my-1 h-px bg-slate-200 dark:bg-slate-700', className)}
    {...props}
  />
));
DropdownMenuSeparator.displayName = DropdownMenuPrimitive.Separator.displayName;

const DropdownMenuLabel = React.forwardRef<
  React.ElementRef<typeof DropdownMenuPrimitive.Label>,
  React.ComponentPropsWithoutRef<typeof DropdownMenuPrimitive.Label> & {
    inset?: boolean;
  }
>(({ className, inset, ...props }, ref) => (
  <DropdownMenuPrimitive.Label
    ref={ref}
    className={cn(
      'px-3 py-1.5 text-sm font-semibold text-slate-900 dark:text-slate-100',
      inset && 'ps-8',
      className
    )}
    {...props}
  />
));
DropdownMenuLabel.displayName = DropdownMenuPrimitive.Label.displayName;

export {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuGroup,
  DropdownMenuSeparator,
  DropdownMenuLabel,
};
```
  </action>
  <verify>
    <automated>grep -c "export" /Users/shahafwieder/inclusify/frontend/components/ui/dropdown-menu.tsx</automated>
  </verify>
  <done>Dropdown menu components exported and available</done>
</task>

<task type="auto">
  <name>Task 2: Create UserAvatar and UserDropdown components</name>
  <files>frontend/components/auth/UserAvatar.tsx, frontend/components/auth/UserDropdown.tsx</files>
  <action>
**1. Create `frontend/components/auth/UserAvatar.tsx`:**

Per CONTEXT.md: User initials on hash-based colored background (no Gravatar).

```typescript
'use client';

// Hash function for consistent color per email
function hashCode(str: string): number {
  let hash = 0;
  for (let i = 0; i < str.length; i++) {
    hash = str.charCodeAt(i) + ((hash << 5) - hash);
  }
  return hash;
}

const AVATAR_COLORS = [
  'bg-red-500', 'bg-orange-500', 'bg-amber-500', 'bg-yellow-500',
  'bg-lime-500', 'bg-green-500', 'bg-emerald-500', 'bg-teal-500',
  'bg-cyan-500', 'bg-sky-500', 'bg-blue-500', 'bg-indigo-500',
  'bg-violet-500', 'bg-purple-500', 'bg-fuchsia-500', 'bg-pink-500',
];

interface UserAvatarProps {
  email: string;
  size?: 'sm' | 'md' | 'lg';
  className?: string;
}

export function UserAvatar({ email, size = 'md', className = '' }: UserAvatarProps) {
  // Get first two characters (handles single-char emails)
  const initials = email.slice(0, 2).toUpperCase();

  // Consistent color based on email hash
  const colorIndex = Math.abs(hashCode(email)) % AVATAR_COLORS.length;
  const bgColor = AVATAR_COLORS[colorIndex];

  const sizeClasses = {
    sm: 'w-6 h-6 text-xs',
    md: 'w-8 h-8 text-sm',
    lg: 'w-10 h-10 text-base',
  };

  return (
    <div
      className={`${sizeClasses[size]} ${bgColor} rounded-full flex items-center justify-center text-white font-medium ${className}`}
      aria-label={`Avatar for ${email}`}
    >
      {initials}
    </div>
  );
}
```

**2. Create `frontend/components/auth/UserDropdown.tsx`:**

Per CONTEXT.md: Dropdown with Profile, My Analyses (history), Logout.

```typescript
'use client';

import { useTranslations, useLocale } from 'next-intl';
import Link from 'next/link';
import { LogOut, User, History } from 'lucide-react';
import { useAuth } from '@/contexts/AuthContext';
import { UserAvatar } from './UserAvatar';
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu';

export function UserDropdown() {
  const t = useTranslations('auth');
  const tNav = useTranslations('app');
  const locale = useLocale();
  const { user, logout } = useAuth();

  if (!user) return null;

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <button
          className="flex items-center gap-2 rounded-full focus:outline-none focus:ring-2 focus:ring-pride-purple focus:ring-offset-2"
          aria-label="User menu"
        >
          <UserAvatar email={user.email} />
        </button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end" className="w-56">
        <DropdownMenuLabel className="font-normal">
          <div className="flex flex-col space-y-1">
            <p className="text-sm font-medium">{user.email}</p>
            <p className="text-xs text-slate-500 dark:text-slate-400 capitalize">{user.role}</p>
          </div>
        </DropdownMenuLabel>
        <DropdownMenuSeparator />
        <DropdownMenuItem asChild>
          <Link href={`/${locale}/profile`} className="flex items-center gap-2 cursor-pointer">
            <User className="w-4 h-4" />
            <span>{tNav('profile') || 'Profile'}</span>
          </Link>
        </DropdownMenuItem>
        <DropdownMenuItem asChild>
          <Link href={`/${locale}/history`} className="flex items-center gap-2 cursor-pointer">
            <History className="w-4 h-4" />
            <span>{tNav('myAnalyses') || 'My Analyses'}</span>
          </Link>
        </DropdownMenuItem>
        <DropdownMenuSeparator />
        <DropdownMenuItem
          onClick={() => logout()}
          className="flex items-center gap-2 cursor-pointer text-red-600 dark:text-red-400 focus:text-red-700 dark:focus:text-red-300"
        >
          <LogOut className="w-4 h-4" />
          <span>{t('logout') || 'Logout'}</span>
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

Also add missing translations to en.json and he.json (append to auth section):
```json
"logout": "Logout",
"profile": "Profile",
"myAnalyses": "My Analyses"
```

Hebrew:
```json
"logout": "התנתקות",
"profile": "פרופיל",
"myAnalyses": "הניתוחים שלי"
```

And add to app section in both files:
```json
"profile": "Profile",
"myAnalyses": "My Analyses"
```
Hebrew:
```json
"profile": "פרופיל",
"myAnalyses": "הניתוחים שלי"
```
  </action>
  <verify>
    <automated>ls -la /Users/shahafwieder/inclusify/frontend/components/auth/UserAvatar.tsx /Users/shahafwieder/inclusify/frontend/components/auth/UserDropdown.tsx</automated>
  </verify>
  <done>UserAvatar and UserDropdown components created with correct exports</done>
</task>

<task type="auto">
  <name>Task 3: Update Navbar with auth state integration</name>
  <files>frontend/components/Navbar.tsx</files>
  <action>
Update Navbar to:
1. Import useAuth hook
2. Show "Login" text button when logged out
3. Show UserDropdown when logged in
4. Hide admin link for non-admin users (per CONTEXT.md: hide nav link, 404 if direct nav)

**Key changes to `frontend/components/Navbar.tsx`:**

```typescript
// Add imports
import { useAuth } from '@/contexts/AuthContext';
import { UserDropdown } from '@/components/auth/UserDropdown';

export default function Navbar() {
  const t = useTranslations('app');
  const locale = useLocale();
  const pathname = usePathname();
  const { user, isLoading } = useAuth();

  // Filter navLinks - hide admin for non-admins
  const navLinks = [
    { href: `/${locale}/analyze`, key: 'analyze' },
    { href: `/${locale}/glossary`, key: 'glossary' },
    // Only show admin link if user is site_admin
    ...(user?.role === 'site_admin' ? [{ href: `/${locale}/admin`, key: 'admin' }] : []),
  ];

  // In the right side div, replace the existing content with:
  <div className="flex items-center gap-3">
    <LanguageSwitcher />
    <ThemeToggle />
    {!isLoading && (
      <>
        {user ? (
          <UserDropdown />
        ) : (
          <Link
            href={`/${locale}/login`}
            className="text-sm font-medium text-slate-700 dark:text-slate-300 hover:text-pride-purple transition-colors"
          >
            {t('login') || 'Login'}
          </Link>
        )}
      </>
    )}
    {!pathname.includes('/analyze') && (
      <Link href={`/${locale}/analyze`} className="btn-primary hidden sm:inline-flex">
        {t('cta')}
      </Link>
    )}
  </div>
```

Note: Keep a loading skeleton or nothing while isLoading to avoid flash of unauthenticated content.

Also add "login" to app translations:
- en.json app section: `"login": "Login"`
- he.json app section: `"login": "התחברות"`
  </action>
  <verify>
    <automated>grep -E "useAuth|UserDropdown|site_admin" /Users/shahafwieder/inclusify/frontend/components/Navbar.tsx</automated>
  </verify>
  <done>Navbar shows Login button when logged out, UserDropdown when logged in, admin link hidden for non-admins</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` completes without errors
- Logged out: "Login" text visible in navbar
- Logged in: Avatar with dropdown visible
- Dropdown shows email, role, Profile, My Analyses, Logout
- Non-admin users don't see Admin nav link
- Click Logout clears session
</verification>

<success_criteria>
- Login text button visible when not authenticated
- Avatar with initials visible when authenticated
- Dropdown menu accessible via avatar click
- Admin nav link only visible for site_admin role
- Logout clears auth state and shows Login button
- RTL layout works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-auth-frontend-build-login-register-pages-with-oauth/05.3-03-SUMMARY.md`
</output>
