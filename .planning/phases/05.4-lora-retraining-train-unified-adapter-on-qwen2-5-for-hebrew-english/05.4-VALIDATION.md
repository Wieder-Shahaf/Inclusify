---
phase: 05.4
slug: lora-retraining-train-unified-adapter-on-qwen2-5-for-hebrew-english
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-11
---

# Phase 05.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.4+ (backend framework) + manual validation scripts (ML training) |
| **Config file** | backend/tests/ (pytest) — ML uses standalone scripts |
| **Quick run command** | `python ml/training/evaluate.py --adapter ./adapters/best_config/ --sample 50` |
| **Full suite command** | `python ml/training/evaluate.py --all-configs` |
| **Estimated runtime** | ~10 seconds (quick) / ~60 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `python ml/training/evaluate.py --adapter ./adapters/qwen_r16_d0.1/ --sample 50`
- **After every plan wave:** Run `python ml/training/evaluate.py --adapter ./adapters/best_config/ --full`
- **Before `/gsd:verify-work`:** Best config achieves >80% validation accuracy
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 05.4-01-01 | 01 | 1 | LLM-02 | unit | `pytest ml/training/tests/test_adapter_training.py::test_dataset_loads -x` | ❌ W0 | ⬜ pending |
| 05.4-01-02 | 01 | 1 | LLM-02 | unit | `pytest ml/training/tests/test_adapter_training.py::test_stratified_split -x` | ❌ W0 | ⬜ pending |
| 05.4-02-01 | 02 | 2 | LLM-02 | unit | `pytest ml/training/tests/test_adapter_training.py::test_adapter_trains -x` | ❌ W0 | ⬜ pending |
| 05.4-02-02 | 02 | 2 | LLM-02 | integration | `python ml/training/evaluate.py --adapter ./adapters/qwen_r16_d0.1/ --threshold 0.70` | ❌ W0 | ⬜ pending |
| 05.4-03-01 | 03 | 3 | LLM-02 | integration | `python ml/training/evaluate.py --adapter ./adapters/best_config/ --threshold 0.80` | ❌ W0 | ⬜ pending |
| 05.4-04-01 | 04 | 4 | LLM-02 | integration | `curl http://localhost:8001/v1/models` (verify adapter listed) | ✅ Phase 05.2 | ⬜ pending |
| 05.4-04-02 | 04 | 4 | LLM-02 | smoke | `python ml/inference_demo.py --adapter ./adapters/best_config/ --test-sentence "Gender diversity is being pushed by ideological groups."` | ✅ (adapt existing) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `ml/training/tests/test_adapter_training.py` — stubs for LLM-02
  - Tests: dataset loads correctly, stratified split preserves class balance, training completes without OOM
- [ ] `ml/training/evaluate.py` — post-training evaluation script
  - Computes accuracy, F1 (macro/micro), per-class F1, confusion matrix
  - Accepts `--adapter` path, `--threshold` for pass/fail, `--sample` for quick checks
- [ ] `pip install pytest` in ML venv — if not already present

**Note:** ML training validation is script-based, not pytest-based, due to long-running nature (hours per config). Unit tests cover data preparation; integration tests run post-training.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Grid search runs overnight unattended | LLM-02 | Long-running (9-10 hours) | SSH to VM, run `screen -S training`, start grid search script, detach, check next day |
| T4 GPU memory stays <16GB during training | LLM-02 | Hardware monitoring | Run `watch -n 1 nvidia-smi` during training, verify VRAM <14GB for all configs |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
