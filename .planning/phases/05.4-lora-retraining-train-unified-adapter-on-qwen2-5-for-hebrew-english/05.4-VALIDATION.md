---
phase: 05.4
slug: lora-retraining-train-unified-adapter-on-qwen2-5-for-hebrew-english
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-11
---

# Phase 05.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.4+ (backend framework) + manual validation scripts (ML training) |
| **Config file** | backend/tests/ (pytest) — ML uses standalone scripts |
| **Quick run command** | `python ml/training/evaluate.py --adapter ./adapters/best_config/ --sample 50` |
| **Full suite command** | `python ml/training/evaluate.py --all-configs` |
| **Estimated runtime** | ~10 seconds (quick) / ~60 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run `python ml/training/evaluate.py --adapter ./adapters/qwen_r16_d0.1/ --sample 50`
- **After every plan wave:** Run `python ml/training/evaluate.py --adapter ./adapters/best_config/ --full`
- **Before `/gsd:verify-work`:** Best config achieves >80% validation accuracy
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 05.4-01-01 | 01 | 1 | LLM-02 | unit | `pytest ml/training/tests/test_adapter_training.py::test_dataset_loads -x` | ✅ W1 | ⬜ pending |
| 05.4-01-02 | 01 | 1 | LLM-02 | unit | `pytest ml/training/tests/test_adapter_training.py::test_stratified_split -x` | ✅ W1 | ⬜ pending |
| 05.4-01-03 | 01 | 1 | LLM-02 | unit | `ssh azureuser@52.224.246.238 "source ~/vllm-venv/bin/activate && python -c 'from peft import LoraConfig; from trl import SFTTrainer; print(\"Imports OK\")'"` | ✅ W1 | ⬜ pending |
| 05.4-02-01 | 02 | 2 | LLM-02 | unit | `python -c "from ml.training.config import get_grid, CONFIG; grid = get_grid(); assert len(grid) == 9, f'Expected 9 configs, got {len(grid)}'; print(f'Grid: {grid}')"` | ✅ W2 | ⬜ pending |
| 05.4-02-02 | 02 | 2 | LLM-02 | unit | `python -c "from ml.training.train_qwen_grid import create_lora_config; cfg = create_lora_config(16, 32, 0.1); assert cfg.r == 16; print('Script imports OK')"` | ✅ W2 | ⬜ pending |
| 05.4-02-03 | 02 | 2 | LLM-02 | integration | `ssh azureuser@52.224.246.238 "source ~/vllm-venv/bin/activate && cd ~/inclusify && python -c 'from ml.training.train_qwen_grid import main; print(\"Script ready\")'"` | ✅ W2 | ⬜ pending |
| 05.4-03-01 | 03 | 3 | LLM-02 | unit | `python -c "from ml.training.evaluate import load_adapter_model; print('Evaluation script imports OK')"` | ✅ W3 | ⬜ pending |
| 05.4-03-02 | 03 | 3 | LLM-02 | integration | `ssh azureuser@52.224.246.238 "screen -ls | grep training && echo 'Training started' || echo 'No training session'"` | ✅ W3 | ⬜ pending |
| 05.4-03-CP | 03 | 3 | LLM-02 | manual | Human verifies grid search completion and best config selection | ✅ checkpoint | ⬜ pending |
| 05.4-03-03 | 03 | 3 | LLM-02 | integration | `test -f ml/adapters/best_config.json && cat ml/adapters/best_config.json \| grep -q "val_accuracy" && echo "Best config identified"` | ✅ W3 | ⬜ pending |
| 05.4-04-01 | 04 | 4 | LLM-02 | unit | `grep -q "enable-lora" infra/azure/vllm-vm/vllm.service && grep -q "lora-modules inclusify" infra/azure/vllm-vm/vllm.service && echo "Service file updated"` | ✅ W4 | ⬜ pending |
| 05.4-04-02 | 04 | 4 | LLM-02 | integration | `ssh azureuser@52.224.246.238 "systemctl is-active vllm \| grep -q active && echo 'vLLM running with adapter'"` | ✅ Phase 05.2 | ⬜ pending |
| 05.4-04-03 | 04 | 4 | LLM-02 | smoke | `bash scripts/test-vllm-adapter.sh` | ✅ W4 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

**Wave 0 is complete** — all test files are created in Wave 1 (Plan 01). No Wave 0 plan needed.

**Rationale:** Test files (`test_adapter_training.py`, `evaluate.py`) are created as part of the implementation in Plan 01 and Plan 03 respectively. These are not stubs but fully functional test infrastructure. The sampling continuity is maintained with automated verifications in every task.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Grid search runs overnight unattended | LLM-02 | Long-running (9-10 hours) | SSH to VM, run `screen -S training`, start grid search script, detach, check next day |
| T4 GPU memory stays <16GB during training | LLM-02 | Hardware monitoring | Run `watch -n 1 nvidia-smi` during training, verify VRAM <14GB for all configs |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (N/A - no Wave 0 needed)
- [x] No watch-mode flags
- [x] Feedback latency < 10s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready for execution
