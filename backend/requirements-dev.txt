# Development & test dependencies.
# Installed only in the `development` Docker stage and for local testing.
# The production `runtime` stage installs requirements.txt alone.
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
aiohttp>=3.9.0
moto[s3]>=5.0.0   # in-process S3 mock for blob_storage tests (no MinIO/R2 needed)
